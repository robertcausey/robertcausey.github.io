To get to github use fc and rockall phone
web address for webpage is robertcausey.github.io

However, there is no need to sign onto github to edit documents. Just edit the file index.html in the directory homepag

save changes then open a terminal window in the homepag directory and enter this code:

git add .
git commit -m "somesort of message"
git push
